# Short Response Questions
   1. The most important step that each team members need to do is to add the collaborator to the repo.

   2. Developers use branches to use different a different branch from the main one without affecting it.

   3. Git is a distruted verison control system (Every dev had acess to all changes) Github is a cloud based service that allows developers to store track, and manage changes to thier code. This

   4. The command wont be able to save the work.They mixed up git add and git commit

New code:
git add .
git commit -m "saving work"
git push 